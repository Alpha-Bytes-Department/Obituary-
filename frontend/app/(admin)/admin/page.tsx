import AdminDashboard from "@/Component/admin/AdminDashboard";

/**
 * Wrapper for the admin dashboard route.
 *
 * @returns {JSX.Element} The admin dashboard container.
 */
export default function AdminPage() {
  return <AdminDashboard />;
}
